logging:
  level:
    com.eastict.cloud: debug
  file: eastict.log
  pattern:
    console: "%d{yyyy-MM-dd} == [%thread] == %-5level == %logger{50} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} >>> [%thread] >>> %-5level >>> %logger{50} >>> %msg%n"